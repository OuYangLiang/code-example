spring:
  application:
    name: eureka-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/OuYangLiang/eureka-config
          username: xxx
          password: 'xxx'

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/
    register-with-eureka: true
    fetch-registry: false

server:
  port: 9999
